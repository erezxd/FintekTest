import React from 'react';
import logo from '../images/fintek_logo.png';

const Logo = () => (
    <header className="app-header">
        <img src={logo} alt="Fintek Digital" className="logo" />
    </header>
);

export default Logo;
